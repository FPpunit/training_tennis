import 'package:flutter/material.dart';

class TournamentsDetails extends StatelessWidget {
  const TournamentsDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
